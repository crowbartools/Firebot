import counter from './counter';
import counterChange from './counter-change';
import counterMaximum from './counter-maximum';
import counterMinimum from './counter-minimum';
import counterName from './counter-name';
import counterNewValue from './counter-new-value';
import counterPreviousValue from './counter-previous-value';


export default [
    counter,
    counterChange,
    counterMaximum,
    counterMinimum,
    counterName,
    counterNewValue,
    counterPreviousValue
];