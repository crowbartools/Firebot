"use strict";

const userDB = require("../../database/userDatabase");
const currencyDB = requir("../../database/currencyDatabase");

function addCurrencyToOnlineUsers(currency, amount) {
  userDB.addCurrencyToOnlineUsers(currency.id, value);
}
